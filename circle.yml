checkout:
  post:
    - git submodule update --init
machine:
  node:
    version:
      0.8.2
dependencies:
  override:
    - sudo npm install nodeunit
    - su npm install nodeunit
    - npm install nodeunit
test:
  override:
    - nodeunit unit_test.js